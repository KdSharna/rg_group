trigger:
- main

pool:
  name: My_todo_agent

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'my_service'  # yeh tu service connection ka naam dale
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      echo "Creating Resource Group..."
      az group create --name paddu --location eastus2
  displayName: 'Create Azure Resource Group'
